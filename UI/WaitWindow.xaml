<Window x:Class="SMTool.UI.WaitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMTool.UI"
        mc:Ignorable="d"
        Title="WaitWindow" 
        Height="140" Width="375"
        Loaded="WindowLoaded"
        ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True" Opacity="0.7" Background="Black"
        >
    <Grid>
        <ProgressBar Name="WaitProgressBar" Margin="25,90,25,25" Value="-3" BorderThickness="1" Opacity="0.8"/>
        <TextBlock Name="WaitInfoTextBlock" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Margin="25,15,25,0" Height="60" FontSize="20" Foreground="White"/>
    </Grid>
</Window>
